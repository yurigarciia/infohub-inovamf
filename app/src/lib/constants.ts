/** Turma/semestre corrente (RF-24: usado como filtro por período no
 * painel do admin). Nesta fase de frontend mockado é um valor fixo;
 * no backend real isso vira configuração da coordenação. */
export const CURRENT_COHORT = "2026.2";
