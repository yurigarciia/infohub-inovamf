{
  "name": "infohub-inovamf-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Repositorio raiz do InfoHub -> InovAMF. A aplicacao (Next.js) vive em app/; estes scripts delegam para la, para o projeto continuar subindo com um comando so a partir da raiz.",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "start": "npm --prefix app run start",
    "lint": "npm --prefix app run lint",
    "install:app": "npm --prefix app install"
  }
}
